import React from 'react';
import PostData from '../../PostData/PostData';

const Media = ({post}) => {
    // const {description, time,image_url } = post || {}
    return (
        <div>
            
            <PostData/>
        </div>
    );
};

export default Media;